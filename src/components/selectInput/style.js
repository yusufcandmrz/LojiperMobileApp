import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        backgroundColor: "white",
        borderWidth: 1,
        borderRadius: 10,
        borderColor: "orange",
        margin: 10,
    },
    picker: {
        color: "orange"
    }
})